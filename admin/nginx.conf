server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Po Traefiku: StripPrefix /admin – tu serwujemy pliki SPA z głównego katalogu
    location / {
        try_files $uri $uri/ /index.html;
    }
}
