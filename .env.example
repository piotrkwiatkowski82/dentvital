# Database
POSTGRES_DB=dentvital
POSTGRES_USER=dentvital
POSTGRES_PASSWORD=changeme_strong_password

# Backend
DATABASE_URL=postgresql+asyncpg://dentvital:changeme_strong_password@db:5432/dentvital
CORS_ORIGINS=http://localhost,http://localhost:5173
# Na produkcji z Traefikiem: CORS_ORIGINS=https://dentvital.dotfusion.pl

# SMTP
SMTP_HOST=mail.example.com
SMTP_PORT=587
SMTP_USERNAME=recepcja@dentvital.pl
SMTP_PASSWORD=changeme_smtp_password
SMTP_FROM_EMAIL=noreply@dentvital.pl
SMTP_USE_TLS=true

# Clinic
CLINIC_EMAIL=recepcja@dentvital.pl

# Traefik (przy deployu z docker-compose.traefik.yml)
TRAEFIK_DOMAIN=dentvital.dotfusion.pl
# Sieć, w której działa Traefik (domyślnie traefik_public). Gdy Traefik jest w innym projekcie: np. apartament340_default
# TRAEFIK_NETWORK=traefik_public
# Resolver certyfikatów w Traefiku (np. myresolver, letsencrypt)
# TRAEFIK_CERT_RESOLVER=myresolver

# Frontend (build-time)
VITE_API_URL=
